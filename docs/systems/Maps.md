# Map And Atlas System

Compatibility pointer: the authoritative system design moved to
[`Atlas.md`](Atlas.md) when the `addons/atlas` shell was created.

Existing typed `WebsiteMapMarker` and `MinecraftProjectionPublisher` behavior
remains documented there as the current marker boundary. Do not extend this
file with a second Atlas design.

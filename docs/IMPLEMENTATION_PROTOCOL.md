# Superseded Elarion Codex Implementation Protocol

Status: **Superseded on 2026-07-30**.

This file is retained as a redirect so historical links do not break. It is not
an active instruction surface.

Current authority and workflow are:

1. `RULES.md` for permanent policy and ownership invariants;
2. `AGENTS.md` for the operational workflow;
3. `docs/ai/CURRENT_STATUS.md` for the bounded handoff snapshot;
4. `docs/ai/routes.json` and `dev/tools/ai-context.ps1` for optional focused
   context routing;
5. `PLAN.md` for the single active direction and `TODO.md` for unfinished
   implementation work.

Broad repository work must use evidence-driven discovery when the context
router refuses insufficient coverage. It must not restore this protocol's old
mandatory `PLAN`/`RULES`/`INDEX`/`TODO` read sequence.
